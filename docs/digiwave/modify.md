整体结构即将迎来大改，几乎等同于代码重构，我建议可以把一些common的东西从wave_engine中再提取出来
# 0.图幅显示
## 0.1
图幅显示不再有终点，无限向后延伸。但是存储时为了节约存储空间，以最后信号都不变的位置为终点。注意如果加载了存储的json，然后继续往后画，注意需要更新存储的终点。

# 1.时钟类型信号
## 1.1
不再有专门的时钟信号，时钟信号使用普通的binary类型+repeat的方式实现
## 1.2
所有信号存储都是存储起始位置和终点位置来确定的，不再有时钟的概念。注意缩放时存储的其实位置和终点位置不变，显示时只是在上面乘以一个系数，这两者不能混淆。
# 2.普通二值信号（重要）
现在的绘制交互方式有以下几种
## 2.1选中信号
在View模式下，鼠标或者上下键选中信号，此时底部状态栏模式还是View模式，同一行的提的详细的提示栏（小字）显示：xxx(信号名) slelected。
## 2.2移动信号编辑光标 
新建二值信号或者选中二值信号，此时光标（窄窄的光标,边框不透明，内部是带一定透明度的，表示当前编辑时刻）。如果是新建的信号，就在0处的位置。如果不是新建的信号，默认也在0处的位置。对于不是新建的信号，你可以用鼠标点击或者键盘左右选择一个信号切换的位置。
## 2.3进入编辑模式
然后按下e，此时状态栏显示Edit模式，光标开始闪烁。
## 2.4编辑操作
在编辑模式下，此时按下1或者0或x或z，表示变化后的信号值是0，1，x或者z，此时就会在光标的右上方显示一小段（注意，是在光标的右上方显示一小段，而不是在波形上显示，因为此时还没有画）。这时你比如按下1再按下0，说明你接下来要画0而不是画1之前1是按错了，也就是后面的覆盖前面的。光标右上角的指示也随之变化。
## 2.4.1绘制信号方法1
光标右上角指示出现，表示将要绘制某信号时，鼠标在相应位置点击，该值就会延续到哪里。注意，如果该编辑位置后面有信号时，会将那些信号向后推。在Edit模式下，按回退键，就会删除该位置到上一个信号翻转位置之间的信号。注意，内部存储只是存储信号切换(翻转)位置，因此如果你在一段高电平后面仍然添加高电平，原来的那个编辑位置就不存在了。
## 2.4.2绘制信号方法2
光标右上角指示出现，表示将要绘制某信号时，按下回车，会弹出来信号持续到哪里，有ns/us/ms可以选择，支持小数。默认的绘制模式是不缩放情况下，10px代表1ns，这个可以在顶部的显示scale里面进行调节。总之要保证在不缩放的情况下scale、ns和信号变化位置关系要正确。
## 2.4.3绘制信号方法3
如果吸附功能开启的情况下（吸附模式就是打开吸附模式并指明吸附信号，此时该信号所有跳变沿都会出现栅格），此时在编辑模式下，光标右上角指示出现时，按下空格，就会自动延续到下一个栅格位置。
## 2.4.3绘制信号方法补充说明
如果你为了想表示到clk上升沿之后一小会时间信号再变化，此时你就可以先开启clk栅格，空格到clk上升沿为止，然后再回车，输入延迟时间，相当于两次绘制实现了这个feature

## 2.5编辑模式的其他feature
为了适应多次编辑，一次信号绘制完成后不会退出编辑模式，而且默认光标右上角就是上次的设置。
## 2.6退出编辑模式
在Edit模式下，你按esc又能退回View模式

# 需要你做的
---------------------------------------
## V1.0改进建议
很好，现在功能完成的不错，有以下几个改进点
1.现在选中信号，在view模式下，我看不到光标（即不闪烁的光标，我看不到）
2.进入Edit模式下，我看的到光标，但是光标太细了，我希望稍微有点粗度，类似长方体的样子，但是本着光标不遮挡波形，我希望是外轮廓是不透明的，内部带一定的透明度
3.现在有个bug就是edit模式下按回车，那个弹出来选时延的面板不会自动消失，而鼠标手动点击确认时才会消失，这个更正一下，并且注意下次弹出时，自动沿用上次的输入结果
4.注意，信号一开始不应该已经绘制，应该把当前你画的所谓x态的pattern，作为没有绘制的样子。然后x态应当代表0/1不确定，所以应该用之前的bus的那种x态的pattern来绘制。
---------------------------------------
---------------------------------------
## V2.0改进建议
很好，继续改进
1.View模式下，只能选中信号切换点（包括0位置），点击其他位置是无效的。
2.View模式下，希望蓝色的光标大小能和Edit模式下的一样。
3.希望可以在X态里面打上斜线
---------------------------------------
---------------------------------------
## V3.0改进建议
很好，继续改进
1.待写这个字样比较丑，希望可以设置为高电平就是一个直线上面写个H，低电平就是一个直线上面写个L，其他的也类似。
2.注意每个波形的高度=波形本体高度加预留高度，现在预留高度=给Edit info预留的高度，让edit模式的提示显示在右上角，显示在Ediy info预留区
3.为了更加人性化，二值信号上一次绘制如果是0或者1，下次绘制默认是1或者0
4.为了方便绘制时钟之类的信号，Edit新增一个Repeat的类型，在Edit模式下按下r，下面提示栏里就是选择Repeat起点，然后鼠标点击选择，该位置一定要是跳变沿，再选择终点，也是类似的。注意，选择时需要有绿色竖向的虚线作为辅助线，选择完毕之后呢，弹出repeat number输入即可，这个repeat number也是默认保留上次的值。注意，repeat之后绘制完毕的信号，存储模式应当是和普通信号一样的，即repeat只是做个翻译，本质上内部还是翻译成了多次绘制的命令
---------------------------------------
---------------------------------------
## V4.0改进建议
很好，继续改进
1.字体采用Consolas，edit模式下提示的X有点丑，优化一下
2.增强repeat模式的反馈，选择起点后应当起点那个边暂时加粗高亮，然后选择好终点后，起点到终点这一段被加粗高亮，然后你可能理解错我的意思了，我的意思是选中repeat这一段作为pattern，插入到当前Edit所在的位置
---------------------------------------
---------------------------------------
## V5.0改进建议
很好，继续改进
1.一个问题，Edit info的字体好像还不是consolas
2.我希望使用空格来编辑波形时，不要和回车一样，因为空格默认是希望保持的，就保留上一次的绘值的选择
3.为了连接的平滑性，希望把信号x的pattern边界改成方的，不必使用六边形的pattern
4.一些UI的修复，把添加二值信号改成图形化的UI，好看简洁平滑，不要实用Emoj，你可以画一个
5.Edit info的x像一段bus，形状再优化一下吧
6.底部提示栏里面，可以给提示栏在当前Bar上专门扣一个带倒角，底色是浅色的区块，更加显眼。并且注意对齐，现在光标位置字样和Scale是不对齐的，Scale字样位置略高一些
7.吸附目标这些字样可以简化，用blender里面类似吸铁石的那个图标，你可以绘制一个出来，不要用Emoj。工具栏面板以简洁为准，绘制一堆图标，然后鼠标移动过去会有提示，这样是最好的
---------------------------------------
---------------------------------------
## V6.0改进建议
很好，继续改进
1.磁铁的图标不好看，改的像个磁铁吧
2.现在合入BUS的功能，注意，添加信号这个按钮就需要改进了，可以改成有下拉，下拉有二值信号和bus信号，这两个图标也需要你绘制一下
3.合入DigiWave旧版本里面的c键打开Property的功能，现在二值信号和bus有颜色、粗细，信号名等peoperty，bus还有十六进制、十进制的property
4.合入BUS时，值得留意的一个问题就是，我如何输入bus的值或者编辑bus的值？我觉得需要对之前的架构做个细微调整。之前的Edit模式需要改成insert模式，用i键进入，因为之前本质上都是插入。而新增一个真正的Edit模式，按下e，当然如果在信号的存储末尾（或者在开头，当前信号为空的信号），Edit模式等同于insert模式。Edit模式本质上就是修改当前光标所在的value，修改完后光标不会移动。
5.那对于Bus来说，insert模式和Edit模式下，如何确定bus的value呢?在edit和insert模式下，默认会聚焦到底下一个bus value的输入窗口，x或者z是预留的，注意bus的z是需要六边形的。输入其他值就是bus显示的值，此时如果点击鼠标，或者按下回车，或者按下空格就会绘制出来，所以注意空格是被捕获用于绘制的，不能用于bus value
---------------------------------------
---------------------------------------
## V7.0改进建议
1.加入参考代码里面的ungroup功能
2.加入参考代码里面的标尺和arrow功能
3.加入参考代码里面的text功能
4.加入调整scale功能
5.注意标尺和text的自动碰撞需要和原代码保持一致
6.加入原代码的黑白模式，可以在右上角绘制一个打印机或者什么按钮，按一下就进入黑白渲染模式
---------------------------------------
---------------------------------------
## V8.0改进建议
1.无法添加信号
2.view mode光标颜色变成了橙色，之前是蓝色，变回去吧
3.标尺和arrow都异常，无法吸附到相应信号的边界。
4.底部的xxx selected也scale提示用一样的字体和颜色
5.现在scale还是不能调整
---------------------------------------
---------------------------------------
## V9.0改进建议
1.view模式下上下键无法切换选中的信号，希望添加一些反馈，现在我都不知道选中了信号的哪个切换边界。注意标尺和arrow只能选则信号的边界。arrow重新绘制，绘制一个弧线箭头的图标
2.bus信号insert卡死，insert模式下无法输入
3.insert或者edit模式下，应当屏蔽点击信号外其他信号。
---------------------------------------
---------------------------------------
## V10.0改进建议
1.view的Edit info显示在右上角容易冲突，还是显示在下面吧
2.view模式下蓝色光标有问题啊，改成和Edit、insert类似的光标，只不过不闪烁
3.增强arrow和标尺的反馈，选中第一条边时，为那条边增加高亮（类似repeat选中一条边的高亮逻辑）
---------------------------------------
---------------------------------------
## V11.0改进建议
1.点击空白处应当取消标尺和arrow的高亮
2.现在text的stick 模式不会点击折叠不会生效，这里还有些bug
---------------------------------------